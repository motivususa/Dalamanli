[build]
  base      = "winXP"
  command   = "npm install --legacy-peer-deps && (cd ../ipod-classic && npm ci && npm run build && rm -rf ../winXP/public/ipod && mkdir -p ../winXP/public/ipod && cp -r out/* ../winXP/public/ipod/) && npm run build"
  publish   = "winXP/build"
  functions = "netlify/functions"

[[plugins]]
  package = "@netlify/plugin-functions-install-core"

[build.environment]
  NPM_FLAGS   = "--legacy-peer-deps"
  NODE_VERSION = "18"

[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
